import React from "react";
import "./main.css";

const FriendCard = props => (
  <div className="card">
    <div className="img-container">
      <img
        alt={image.name}
        src={image.image}
      />
    </div>
  </div>
);

export default Main;
